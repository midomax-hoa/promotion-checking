# Secrets and database backups
.env
.env.local
.env.production
backups/
*.dump
*.sql.gz
*.tar.gz

# Per-machine compose tweaks. Only the .example is tracked.
docker-compose.override.yml

# Internal import software - reference only, not part of this project
check-promotion/

# Real business data - never push to the repository
promotion*.xlsx
*.xlsx
!test/fixtures/*.xlsx

# Uploaded Excel files kept for report export (see phase 08)
.uploads/
.test-uploads/
uploads/

# Build output
src/generated/
node_modules/
.next/
out/
build/
next-env.d.ts

# AI assistant workspaces - local tooling, not part of the project
.claude/
.opencode/
.agents/
.agent/
.cursor/
.playwright-mcp/

# Misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# Temporary Office lock files
~$*
