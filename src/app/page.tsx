/**
 * Temporary home page. The real screens arrive in phases 05-07.
 */
export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-3xl flex-col justify-center gap-4 p-8">
      <h1 className="text-2xl font-semibold">Công cụ kiểm tra file khuyến mãi</h1>
      <p className="text-muted-foreground">
        Nền tảng dự án đã sẵn sàng. Các màn hình kiểm tra file, đối soát sau import và cấu hình luật
        sẽ được bổ sung ở những giai đoạn sau.
      </p>
    </main>
  )
}
